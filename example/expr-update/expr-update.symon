var {
    num: number;
    str: string;
    str2: string;
    str3: string;
}

signature data {
    n: number;
}

data(n)*;
data( n | | num := n + 10; str := str2; str2 := str3 );
data( n | num > n && str != "hoge" )
